# FlashPhoto

## Configuration 
To configure the project, do the following:

    cd config
    ./configure --enable-shared=no --prefix=`realpath ../ext`

## Building
To build FlashPhoto, do the following:

    make
   
